# drm

Web scraping project using [Scrapling](https://github.com/D4Vinci/Scrapling).

## Setup

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## Usage

See `example.py` for a minimal fetch + parse example:

```bash
python example.py
```
